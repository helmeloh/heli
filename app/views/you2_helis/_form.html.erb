<%= form_for(@you2_heli) do |f| %>
  <% if @you2_heli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@you2_heli.errors.count, "error") %> prohibited this you2_heli from being saved:</h2>

      <ul>
      <% @you2_heli.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
